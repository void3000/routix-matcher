Example payload
```
{
  "agent": {
    "id": "{{ faker.random.uuid }}",
    "skills": [
      "aws-dynamo-db",
      "linux",
      "aws-lambda",
      "database"
    ],
    "languages": [
      "en",
      "ch"
    ]
  }
}
```